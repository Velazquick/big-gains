# Phase 4C browser-safe values only. Never add a secret/service-role key.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Static Pages capability only. Runtime authority is a separate Supabase Edge Function secret.
# Missing or false keeps automatic reconciliation code inactive in this build.
BIG_GAINS_AUTOMATIC_RECONCILIATION=false
